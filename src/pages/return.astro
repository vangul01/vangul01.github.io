<!-- ---
import { stripe } from "../lib/stripe";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "Checkout | VANGULAR";
const keywords = "checkout, stripe, payment";

async function getSession(sessionId: string) {
  const session = await stripe.checkout.sessions.retrieve(sessionId);
  return session;
}

const sessionID = Astro.url.searchParams.get("session_id");
const session = await getSession(sessionID!);
---

<BaseLayout pageTitle={pageTitle} keywords={keywords}>
  <Header />
  <main>
    <section class="content-container">
      <div class="heading">
        <h2>Thanks for your order!</h2>
      </div>
      <div class="content-container">
        {session.status === "open" && <p>Payment did not work.</p>}

        {
          session.status === "complete" && (
            <p>
              We appreciate your business! A confirmation email will be sent to{" "}
              <span id="customer-email" />. If you have any questions, please
              email <a href="mailto:orders@example.com">orders@example.com</a>.
            </p>
          )
        }
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout> -->
