<!-- This is all just for the submit message at the bottom. Same case for NewsletterIsland-->
<!-- import { useState } from "react";
import "../styles/global.css";

export default function ContactFormIsland() {
  const [message, setMessage] = useState("");

  async function handleSubmit(e) {
    e.preventDefault();
    const formData = new FormData(e.target);
    const data = Object.fromEntries(formData.entries());
    // form field names in your markup: "first-name", "last-name", "email", "message"
    const payload = {
      firstName: data["first-name"] || "",
      lastName: data["last-name"] || "",
      email: data.email || "",
      message: data.message || "",
    };
    setMessage("Sending Message...");
    try {
      const response = await fetch("/.netlify/functions/contact-form", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload),
      });
      const json = await response.json();
      if (!response.ok) throw new Error(json.message || "Failed to subscribe");
      setMessage(
        json.message || "Thanks for contacting us! We'll get back to you soon."
      );
      e.target.reset();
    } catch (error) {
      console.log("Contact form error:", error);
      setMessage(error.message || "Message failed to send. Please try again.");
    }
  } -->

<form
      class="contact-form"
      name="contact"
        method="POST"
      data-netlify="true"
      netlify-honeypot="bot-field"
    >
      <input type="hidden" name="form-name" value="contact" />
      <p class="hidden">
        <label>
          Don't fill this out if you're human: <input name="bot-field" />
        </label>
      </p>

      <div class="form-field">
        <input
          type="text"
          id="contact-first-name"
          name="first-name"
          placeholder=" "
          autocomplete="given-name"
          required
        />
        <label for="contact-first-name">First Name</label>
      </div>

      <div class="form-field">
        <input
          type="text"
          id="contact-last-name"
          name="last-name"
          placeholder=" "
          autocomplete="family-name"
          required
        />
        <label for="contact-last-name">Last Name</label>
      </div>

      <div class="form-field">
        <input
          class="email"
          type="email"
          id="contact-email"
          name="email"
          placeholder=" "
          autocomplete="email"
          required
        />
        <label for="contact-email">Email</label>
      </div>

      <div class="form-field">
        <textarea
          id="contact-message"
          name="message"
          placeholder=" "
          rows="5"
          cols="50"
          required
        ></textarea>
        <label for="contact-message">Message</label>
      </div>

      <button
        type="submit"
        class="button button-primary"
        aria-label="Submit Contact Form"
      >
        Submit
      </button>
      <!-- <div>{message}</div> -->
    </form>

